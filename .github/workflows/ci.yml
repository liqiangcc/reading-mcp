name: CI

on:
  push:
  pull_request:

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Format
        id: format
        run: cargo fmt --all -- --check

      - name: Capture rustfmt patch
        if: failure() && steps.format.outcome == 'failure'
        run: |
          cargo fmt --all
          git diff --binary > rustfmt.patch

      - name: Upload rustfmt patch
        if: failure() && steps.format.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: rustfmt-patch
          path: rustfmt.patch
          if-no-files-found: error
          retention-days: 1

      - name: Clippy
        id: clippy
        shell: bash
        run: |
          set -o pipefail
          cargo clippy --locked --all-targets --all-features -- -D warnings 2>&1 | tee clippy.log

      - name: Upload Clippy log
        if: failure() && steps.clippy.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: clippy-log
          path: clippy.log
          if-no-files-found: error
          retention-days: 1

      - name: Test
        id: test
        shell: bash
        run: |
          set -o pipefail
          cargo test --locked --all-features 2>&1 | tee test.log

      - name: Upload test log
        if: failure() && steps.test.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: test-log
          path: test.log
          if-no-files-found: error
          retention-days: 1

  tlpi-source-map-probe:
    if: github.event_name == 'pull_request' && github.head_ref == 'agent/use-cloud-runner'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CONTROL_PLANE_API_KEY: ${{ secrets.CONTROL_PLANE_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      TLPI_PROBE_MODEL: gpt-5.6
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Invoke reading-mcp through the OpenAI product tunnel
        run: python3 .github/scripts/tlpi_source_map_probe_v4.py

      - name: Upload sanitized source result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tlpi-source-map-probe
          path: tlpi-probe.json
          if-no-files-found: error
          retention-days: 1
