name: TLPI source map read-only probe

on:
  pull_request:
    branches:
      - main
    paths:
      - scripts/tlpi_responses_probe.py
      - .github/workflows/tlpi-source-map-probe.yml

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      CONTROL_PLANE_API_KEY: ${{ secrets.CONTROL_PLANE_API_KEY }}
    steps:
      - uses: actions/checkout@v4

      - name: Call Reading MCP through Responses native tunnel support
        shell: bash
        run: python3 scripts/tlpi_responses_probe.py

      - name: Upload sanitized structural evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tlpi-source-map-responses-probe
          path: tlpi-responses-probe.json
          retention-days: 1
          if-no-files-found: error
